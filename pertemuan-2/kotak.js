function setup() {
  createCanvas(640, 480);
  background('rgb(46,121,5)');
  noStroke();
  fill('yellow');
  rectMode(CENTER);
  rect(width/2, height/2, 200, 200); // menggambar persegi pada posisi tengah-tengah canvas
}

function draw() {
  // Tambahkan kode animasi atau interaksi di sini jika diperlukan
}